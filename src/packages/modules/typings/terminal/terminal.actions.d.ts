import { Action } from "redux-actions";
import { TerminalLine } from "./terminal.models";
declare const _default: {
    pushLine: (payload: TerminalLine) => Action<TerminalLine>;
};
export default _default;
